---
description: Show adr CLI usage
---

Dot-form alias for the **adr-tools** skill.

Run `adr help [command]` in the shell, where `$ARGUMENTS` is the optional command name. See `skills/adr-tools/commands.md`.
